---
description: Store a fact in persistent memory, or list memories if none given
---

Call the native skill tool with name "remember". There is no remember tool.
Follow that skill using read/edit on ~/.agents/memory/MEMORY.md.

If `$ARGUMENTS` is empty, list the existing memories below and stop.
Otherwise store this as one memory entry in ~/.agents/memory/MEMORY.md: $ARGUMENTS
